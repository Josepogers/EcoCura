import { StyleSheet } from "react-native";
const styles = StyleSheet.create({
  buttonVoltar: {
    justifyContent: "center",
    alignItems: "center",
    width: 150,
    height: 50,
    borderRadius: 50,
    left: 0,
    top: 0,
    position: "relative",
  },
});

export { styles };
