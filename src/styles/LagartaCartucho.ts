import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  SearchBar: {
    borderColor: "#ccc",
    borderWidth: 1,
    borderRadius: 10,
    paddingHorizontal: 10,
    paddingVertical: 10,
    width: "80%",
    marginTop: 20,
    marginBottom: 20,
  },
});

export { styles };
